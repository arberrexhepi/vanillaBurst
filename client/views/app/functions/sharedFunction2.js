window.sharedFunction2 = async function sharedFunction2(runFunction){

    if(window.runFunction ==="functionBurst"){

        window.domFunction='sharedFunction2'
         await window.miniDOM(window.sharedFunction2Config(), domFunction, quitGame)

         function quitGame() {
            const closeButton = document.getElementById('quitGameButton');
            closeButton.addEventListener('click', quitGameButton);

            async function quitGameButton() {

               window.vanillaGo("app");
               
            }

        }
    }   
    else {
        console.warn("sharedFunction1 view: runFunction not set, halting execution.");
    }
}
//define functions here
